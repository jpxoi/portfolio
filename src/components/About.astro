---
import { aboutInfo } from '../data/about'
import AboutBox from './AboutBox.astro'
import PageSection from './PageSection.astro'
import SectionHeading from './SectionHeading.astro'
import SiteContainer from './SiteContainer.astro'
import { CDN_URL } from '../constants'

const imgAvif = `${CDN_URL}/portfolio/about/about.avif`
const imgWebp = `${CDN_URL}/portfolio/about/about.webp`
const imgFallback = imgWebp
---

<PageSection id='about' class='pt-10! lg:pt-14!'>
  <SectionHeading subtitle='Who I Am' title='About Me' />

  <SiteContainer class='grid gap-10 lg:grid-cols-2 lg:items-center lg:gap-x-16'>
    <picture
      class='animate-floaty xs:w-55 mx-auto w-32.5 justify-self-center rounded-3xl motion-reduce:animate-none lg:mx-0 lg:w-87.5'
    >
      <source srcset={imgAvif} type='image/avif' />
      <source srcset={imgWebp} type='image/webp' />
      <img loading='lazy' src={imgFallback} alt='Memoji of Jean Paul Fernandez' width={350} height={350} />
    </picture>

    <div class='text-center lg:text-left'>
      <div
        class='xs:grid-cols-3 mb-8 grid grid-cols-2 gap-2 sm:mx-auto sm:grid-cols-[repeat(3,140px)] sm:justify-center lg:mx-0 lg:justify-start'
      >
        {aboutInfo.map((info) => <AboutBox {...info} />)}
      </div>

      <p class='text-copy text-small mb-4 leading-relaxed text-pretty lg:mb-6'>
        Full-stack software engineer and Computer Science student at The University of Manchester (UoM), focused on
        building scalable web platforms, AI-powered applications and cloud-based digital products.
      </p>
      <p class='text-copy-muted text-small leading-relaxed text-pretty'>
        I work across modern web technologies, backend systems, databases and infrastructure, combining technical
        problem-solving with a product-oriented approach to create practical, reliable and user-focused software for
        real-world business needs.
      </p>
    </div>
  </SiteContainer>
</PageSection>
