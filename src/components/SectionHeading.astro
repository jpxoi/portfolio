---
interface Props {
  title: string
  subtitle?: string
}

const { title, subtitle }: Props = Astro.props
---

{subtitle ? <span class='text-copy dark:text-copy-muted text-smaller block text-center'>{subtitle}</span> : null}
<h2 class='text-primary text-h2 mb-8 text-center lg:mb-14'>{title}</h2>
