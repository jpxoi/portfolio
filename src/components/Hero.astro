---
import { heroSocialLinks } from '../data/socials'
import { STATIC_CONTENT_URL } from '../constants'
import HeroSocialLink from './HeroSocialLink.astro'
import { Mouse } from '@boxicons/react'

const imgAvif = `${STATIC_CONTENT_URL}/media/images/profile/profile.avif`
const imgWebp = `${STATIC_CONTENT_URL}/media/images/profile/profile.webp`
const imgFallback = imgWebp
---

<section class='home section' id='home'>
  <div class='custom-container relative grid gap-5! pt-8'>
    <div class='text-center'>
      <span class='home__greeting'>Hello, I&apos;m</span>
      <h1 class='text-(length:--biggest-font-size)'>Jean Paul Fernandez</h1>
      <h2 id='dynamic-header' class='home__education min-h-6'>Software Engineer</h2>

      <div class='home__buttons'>
        <a
          href={`${STATIC_CONTENT_URL}/pdf/Jean_Paul_Fernandez_CV.pdf`}
          target='_blank'
          download='Jean_Paul_Fernandez_CV.pdf'
          rel='noreferrer'
          class='button button--ghost'
        >
          Download CV
        </a>

        <a href='#about' class='button'>About Me</a>
      </div>
    </div>

    <div class='home__handle'>
      <picture class='home__img'>
        <source srcset={imgAvif} type='image/avif' />
        <source srcset={imgWebp} type='image/webp' />
        <img
          loading='eager'
          fetchpriority='high'
          src={imgFallback}
          alt='Stand up Memoji of Jean Paul Fernandez'
          class='home__img'
          width={220}
          height={381.05}
        />
      </picture>
    </div>

    <div class='home__social'>
      {heroSocialLinks.map((link) => <HeroSocialLink {...link} />)}
    </div>

    <a href='#about' class='home__scroll'>
      <Mouse className='size-6' />
      <span class='-rotate-90 text-(length:--smaller-font-size)'>Scroll Down</span>
    </a>
  </div>
</section>
<script>
  const headerTexts = ['Software Engineer', 'Full-stack Developer', 'AI Engineer']

  const dynamicHeader = document.getElementById('dynamic-header') as HTMLElement

  let textIndex = 0
  let charIndex = headerTexts[0].length - 1
  let isDeleting = false
  let pauseAfterTyped = 2000 // pause before deleting

  function randomBetween(min: number, max: number) {
    return Math.floor(Math.random() * (max - min + 1) + min)
  }

  function typeWriter() {
    const fullText = headerTexts[textIndex]
    let delay

    if (!isDeleting) {
      dynamicHeader.textContent = fullText.substring(0, charIndex + 1)
      charIndex++
      if (charIndex === fullText.length) {
        isDeleting = true
        setTimeout(typeWriter, pauseAfterTyped)
        return
      }
      // Typing speed: random between 60–80ms
      delay = randomBetween(60, 80)
    } else {
      dynamicHeader.textContent = fullText.substring(0, charIndex - 1)
      charIndex--
      if (charIndex === 0) {
        isDeleting = false
        textIndex = (textIndex + 1) % headerTexts.length
      }
      delay = randomBetween(80, 100)
    }
    setTimeout(typeWriter, delay)
  }

  typeWriter()
</script>
