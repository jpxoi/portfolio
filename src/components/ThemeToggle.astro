---
import { Moon, Sun } from '@boxicons/react'

const iconClass = 'size-5 transition-all motion-reduce:transition-none'

const sunClass = [iconClass, 'scale-100 rotate-0', 'dark:scale-0 dark:-rotate-90'].join(' ')

const moonClass = [iconClass, 'scale-0 rotate-90', 'dark:scale-100 dark:rotate-0'].join(' ')
---

<button
  type='button'
  id='theme-button'
  class='relative inline-flex size-9 shrink-0 cursor-pointer items-center justify-center text-(--text-color) transition-colors duration-200 ease-in-out focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-(--first-color) active:scale-[0.98] [@media(hover:hover)]:hover:text-(--first-color)'
  aria-label='Toggle color theme'
>
  <span class='relative block size-5' aria-hidden='true'>
    <Sun className={sunClass} color='currentColor' />
    <span class='pointer-events-none absolute inset-0 flex items-center justify-center'>
      <Moon className={moonClass} color='currentColor' />
    </span>
  </span>
</button>

<script>
  import { toggleTheme } from '../lib/theme'

  document.getElementById('theme-button')?.addEventListener('click', () => {
    toggleTheme()
  })
</script>
